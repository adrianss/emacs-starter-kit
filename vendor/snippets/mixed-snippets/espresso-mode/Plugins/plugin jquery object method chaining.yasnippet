# -*- mode: snippet -*-
# key: plug
# contributor: Adrian Sanchez
# name: jquery obj method chain
# expand-env: (yas/also-auto-indent-first-line)
# --
(function(\$) {
  \$.fn.${1:fnName} = function(options) {
    var opts = \$.extend({}, \$.fn.pulse.defaults, options);
    return this.each(function() {
      // do something with \$(this) $0
    });
  };

  // plugin default options
  \$.fn.$1.defaults = {
     opt1: "slow",
     opt2: 2
  };
})(jQuery);